# 동일 설정 6 런의 잡음 바닥 — 표류도 계단도 아니고, n=3 완전분리 규칙의 검정력은 8.5 %

- id: finding/055 · date: 2026-09-06 · author: claude opus5 exp-run
- harness: omx · to: all
- subject: adjudication-rule-power · supersedes: none
- topic: decision
- confidence: high · status: needs-experiment
- verified: none · keywords: tilt30, D1, noise-floor, adjudication-rule, power, false-positive, drift, machine-state
- summary: D1 실행: 동일 설정 6 런 len_r 폭 0.0366 · 2D sd 0.828 m(CV 27 %). 사전등록 H1(계단)·H2(표류) 모두 불성립 — 시간 순서도 머신 상태 상관도 없는 기억 없는 잡음. 이 잡음에서 n=3 두 축 완전분리 규칙은 오탐 3.0 % · 검정력 8.5 % 라, 12축 1채택 성적이 '전부 실효과' 와 구별되지 않고 기각 11 건은 거의 무정보다.

proposal `next-20260906-062322`(D1) 실행. 동일 설정 6 런 연속(`d1base`~`d6base_260906_*`), 런 직전 머신 상태 스냅샷 동반(`drift.snap.log`, `snap.sh`), 판정 `driftdiag.py`.

## 사전등록 두 예측 모두 불성립

- H1(계단형/막힌 상태) 예측: len_r 폭 <= 0.02 AND |rho(순서)| < 0.5 → **폭 0.0366 으로 불성립**.
- H2(단조 표류) 예측: rho(순서) <= -0.8 AND 폭 >= 0.03 AND 스냅샷 필드 불변 → **rho +0.086(p 0.872) 으로 불성립**.
- 관측된 것은 제3의 구조다: **폭은 크고(H2 쪽) 시간 순서는 없다(H1 쪽)**. 스냅샷 9개 필드 중 len_r 과 유의하게 붙는 것이 없다(최대 `mem_used_mb` rho -0.657, p 0.156, n=6; `cpu_mhz_mean` rho -0.029, `load` +0.257). 즉 **기억 없는 런 간 확률변동**이며, 표류도 머신 상태 결함도 아니다.

## 잡음 바닥 (동일 설정 n=6)

| 지표 | median | sd | 범위 | 폭 | CV |
|:--|--:|--:|:--|--:|--:|
| 2D mean | 3.061 | 0.828 | [2.151, 4.369] | 2.218 | 27.2 % |
| ATE | 2.930 | 0.547 | [2.106, 3.374] | 1.268 | 19.2 % |
| len_r | 0.9202 | 0.0121 | [0.9080, 0.9446] | 0.0366 | 1.3 % |

축 사이: 2D~ATE Pearson +0.706 · ATE~len_r Spearman **-0.714**(압축이 덜한 런이 ATE 도 좋다) · 2D~len_r +0.029(len_r 은 2D 를 끌지 않는다).

## 판정 규칙에 대한 귀결 — 여기가 이 finding 의 본론

관측 sd·상관을 가진 이변량정규에서 효과 0 인 3+3 짝블록을 20 만 번 뽑아 재현:

- 단일 축 완전분리 오탐률 **9.98 %**(이론값 2/C(6,3)=10 % 와 일치 — 이 규칙은 데이터와 무관하게 10 % 다).
- 두 축 동시·같은 방향 완전분리 오탐률 **3.04 %**.
- 검정력: finding/048 이 실제로 본 효과 0.598 m(0.72 sd)에서 **8.5 %**, 0.30 m 에서 **3.9 %**.

따라서 (1) **12 축을 시도했으므로 채택 1 건 중 적어도 하나가 오탐일 확률은 31.0 %**. (2) 12 축이 *전부* 0.6 m 실효과였더라도 이 규칙의 기대 채택 수는 **1.0 건** — 실제 관측 1/12 와 같다. 즉 **1/12 라는 성적은 '아무것도 안 통한다' 와 '전부 통하는데 규칙이 못 본다' 를 구별하지 못한다.** (3) 기각된 11 축은 검정력 <= 8.5 % 이므로 거의 정보가 없다 — finding/054 가 pcm5 에 대해 말한 '기각이 아니라 미증명' 이 **닫힌 축 전부로 일반화된다**.

## finding/051 표류에 대한 함의

051 의 base 표류(14 런 rho -0.49, p 0.075)는 D1 이 같은 설정 6 런에서 시간 순서를 전혀 못 찾았고(rho +0.086) 머신 상태 상관도 없다는 점에서, **표류가 아니라 이 기억 없는 잡음의 한 실현**으로 읽는 것이 맞다. 원인 탐색을 계속할 근거가 약해졌다.

## 한계

n=6 이라 sd 자체의 불확실성이 크다 — 2D sd 95 % CI [0.517, 2.032], ATE [0.342, 1.342]. 오탐률·검정력 수치는 이변량정규 가정 아래의 값이며 점추정 sd 를 쓴다. 방향(규칙이 매우 보수적이다)은 CI 하단에서도 뒤집히지 않지만 정확한 %는 그렇지 않다.

## 사람 결정 필요

프로그램 전역의 채택 규칙(n=3 두 축 완전분리)을 바꿀지는 사용자 결정이다. 이 finding 은 규칙을 바꾸지 않았다. L2 는 이미 승인된 사전등록 부호검정(14 쌍)으로 돌므로 영향받지 않는다.
## Comments
- (2026-09-06, claude opus5 exp-run) 2026-09-06 12:22 L2 실행 중 관측. D1 이 66분 창에서 len_r 의 계단형(H1)을 기각했지만, 같은 날 feat_frames 는 L1->L2 큐 경계(11:48)에서 10830~10915(9런) -> 10500~10600(3런) 으로 계단을 보인다. 프로세스 잔류·shm 누수·CPU 스로틀링·메모리 압박 넷을 배제했고 원인은 미상이다. 모순이 아니라 계단 구조가 정확도 지표가 아니라 처리량 계수에 있다는 뜻이며, D1 의 결론(정확도 산포는 기억 없는 잡음)은 유지된다.

- (2026-09-06, claude opus5 exp-run) 정정 — 위 코멘트의 'feat_frames 계단' 은 성립하지 않는다. L2 11 런까지의 featF 는 10500·10600·10580·10800·10625·10915·10850·10875·10860·10755·10905 로 폭 415 이고, 어제(2026-09-05) 같은 설정의 m 6 쌍 12 런은 10630~10970 폭 340 이다. 즉 넓은 산포가 이 지표의 평소 모습이고, 좁았던 쪽이 D1(폭 85, 6 런)·L1(폭 55, 3 런) 이다. 큐 경계에서 계단을 봤다는 판단은 비정상적으로 좁은 D1/L1 창을 기준선으로 삼은 데서 나온 오독이며, 3 런만 보고 단정한 것이다. 전 런이 유효대역 10175 위이므로 판정에는 영향이 없다. 이 오독 때문에 세션이 만든 '짝 featF 차 200 초과면 오염' 규칙도 임계가 D1 폭에서 유도돼 실제 산포를 오염으로 잡았고, 승인된 n=14 를 줄이지 않도록 민감도 분석으로 격하했다.
